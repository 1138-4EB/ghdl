name: 'snapshot'

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 2 * *'

jobs:

  snapshot:
    runs-on: ubuntu-latest
    steps:

    - uses: actions/checkout@v2
      with:
        fetch-depth: 0

    - run: ./scripts/snapshot.sh
